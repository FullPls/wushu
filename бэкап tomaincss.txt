/* ==========================================
   ГЛАВНАЯ СТРАНИЦА – to_main.css
   ========================================== */
   * {
	margin: 0;
	padding: 0;
	box-sizing: border-box;
  }
  
  body {
	font-family: 'Open Sans', sans-serif;
	background-color: #F5F5F5;
	color: #1A1A1A;
	line-height: 1.5;
	scroll-behavior: smooth;
  }
  
  :root {
	--red: #B22222;
	--red-dark: #8B1E1E;
	--gold: #D4AF37;
	--black: #1A1A1A;
	--gray-dark: #2C2C2C;
	--light-bg: #F5F5F5;
	--white: #FFFFFF;
	--shadow: 0 20px 30px -10px rgba(0, 0, 0, 0.1);
	--transition: all 0.3s ease;
  }
  
  .container {
	max-width: 1280px;
	margin: 0 auto;
	padding: 0 24px;
  }
  
  section {
	padding: 80px 0;
  }
  
  .section-title {
	font-family: 'Montserrat', sans-serif;
	font-size: 2.5rem;
	font-weight: 700;
	text-align: center;
	margin-bottom: 1rem;
	color: var(--black);
  }
  
  .section-title span {
	color: var(--red);
	border-bottom: 3px solid var(--gold);
	display: inline-block;
	padding-bottom: 6px;
  }
  
  .section-subtitle {
	text-align: center;
	color: #555;
	max-width: 700px;
	margin: 0 auto 48px;
	font-size: 1.1rem;
  }
  
  .btn {
	display: inline-flex;
	align-items: center;
	gap: 8px;
	padding: 12px 28px;
	font-weight: 600;
	border-radius: 40px;
	text-decoration: none;
	transition: var(--transition);
	font-family: 'Montserrat', sans-serif;
	cursor: pointer;
	border: none;
  }
  
  .btn-primary {
	background: var(--red);
	color: white;
	box-shadow: 0 4px 8px rgba(178, 34, 34, 0.3);
  }
  
  .btn-primary:hover {
	background: var(--red-dark);
	transform: translateY(-3px);
	box-shadow: 0 8px 20px rgba(178, 34, 34, 0.4);
  }

  .btn-pastel-green {
	--bs-btn-bg: #8fbe8a;
	--bs-btn-border-color: #79aa74;
	--bs-btn-color: #16331b;
	--bs-btn-hover-color: #16331b;
	--bs-btn-active-color: #16331b;
	--bs-btn-active-bg: #7fb679;
	--bs-btn-active-border-color: #679a62;
	display: inline-flex;
	background-color: #8fbe8a;
	color: #16331b;
	border: 2px solid #79aa74;
	box-shadow: 0 4px 12px rgba(70, 120, 65, 0.35);
  }

  .btn-pastel-green:hover {
	--bs-btn-hover-bg: #7fb679;
	--bs-btn-hover-border-color: #679a62;
	background-color: #7fb679;
	color: #1f3b24;
	transform: translateY(-3px);
	box-shadow: 0 8px 20px rgba(70, 120, 65, 0.45);
  }
  
  .btn-outline {
	border: 2px solid var(--red);
	color: var(--red);
	background: transparent;
  }
  
  .btn-outline:hover {
	background: var(--red);
	color: white;
	transform: translateY(-3px);
  }
  
  /* Hero */
  .hero-section {
	position: relative;
	overflow: hidden;
	min-height: 560px;
	border-radius: 28px;
	background-color: #222;
  }

  .hero-section::before {
	content: "";
	position: absolute;
	inset: 0;
	background-image: url("D:/Сайт/shop/public/img/hero/1.jpg");
	background-position: center;
	background-size: cover;
	animation: heroCarousel 18s infinite;
	z-index: 0;
  }

  .hero-section .container {
	position: relative;
	z-index: 2;
  }

  .hero-overlay {
	position: absolute;
	inset: 0;
	background: linear-gradient(90deg, rgba(0,0,0,0.72) 0%, rgba(0,0,0,0.45) 55%, rgba(0,0,0,0.2) 100%);
	z-index: 1;
  }

  .hero-title,
  .hero-subtitle {
	color: white;
	text-shadow: 0 3px 12px rgba(0,0,0,0.55);
  }

  .hero-title {
	font-size: clamp(2.1rem, 4vw, 3.6rem);
	font-weight: 800;
  }

  .hero-subtitle {
	max-width: 620px;
	font-size: 1.1rem;
	margin: 18px 0 28px;
  }

  @keyframes heroCarousel {
	0%, 28% {
	  background-image: url("D:/Сайт/shop/public/img/hero/1.jpg");
	}
	33%, 61% {
	  background-image: url("D:/Сайт/shop/public/img/hero/1.jpg");
	}
	66%, 100% {
	  background-image: url("D:/Сайт/shop/public/img/hero/1.jpg");
	}
  }

  .kungfu-quote {
	background: #f1f3f5;
  }

  .kungfu-quote-card {
	background: white;
	padding: 36px;
	border-left: 6px solid var(--gold);
	text-align: center;
  }

  .kungfu-quote-card h5 {
	color: var(--red);
	font-size: 1.35rem;
	font-weight: 700;
	margin-bottom: 18px;
  }

  .kungfu-quote-card p {
	color: #343a40;
	font-size: 1rem;
	line-height: 1.7;
	margin: 0;
  }

  .hero {
	position: relative;
	min-height: 90vh;
	background: linear-gradient(90deg, rgba(0,0,0,0.75) 0%, rgba(0,0,0,0.4) 60%, rgba(0,0,0,0.1) 100%), url('https://images.unsplash.com/photo-1555597673-b21d5c935865?ixlib=rb-4.0.3&auto=format&fit=crop&w=1950&q=80');
	background-size: cover;
	background-position: center 30%;
	display: flex;
	align-items: center;
	text-align: left;
	color: white;
  }
  
  .hero-content {
	max-width: 650px;
	margin-left: 10%;
	padding: 0 20px;
	animation: fadeInUp 0.9s ease;
  }
  
  .hero h1 {
	font-size: 3.2rem;
	font-family: 'Montserrat', sans-serif;
	font-weight: 800;
	margin-bottom: 20px;
  }
  
  .hero h1 i {
	color: var(--gold);
  }
  
  .hero p {
	font-size: 1.25rem;
	margin-bottom: 32px;
	font-weight: 400;
	opacity: 0.95;
  }
  
  .hero-buttons {
	display: flex;
	gap: 20px;
	flex-wrap: wrap;
  }
  
  @keyframes fadeInUp {
	from { opacity: 0; transform: translateY(30px); }
	to { opacity: 1; transform: translateY(0); }
  }
  
  /* Статистика */
  .stats-grid {
	display: flex;
	flex-wrap: wrap;
	justify-content: center;
	gap: 32px;
	margin-top: 48px;
  }
  
  .stat-card {
	background: white;
	padding: 32px 24px;
	border-radius: 24px;
	text-align: center;
	flex: 1;
	min-width: 180px;
	box-shadow: var(--shadow);
	transition: var(--transition);
	border-bottom: 4px solid var(--gold);
  }
  
  .stat-card:hover {
	transform: translateY(-8px);
  }
  
  .stat-number {
	font-size: 2.8rem;
	font-weight: 800;
	color: var(--red);
	font-family: 'Montserrat', sans-serif;
  }
  
  .stat-icon {
	font-size: 2.2rem;
	color: var(--gold);
	margin-bottom: 16px;
  }
  
  /* Достижения, направления, тренеры, новости – общая сетка */
  .achievements-grid,
  .disciplines-grid,
  .coaches-grid,
  .news-grid {
	display: grid;
	grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
	gap: 32px;
	margin-top: 32px;
  }
  
  .achievement-card,
  .discipline-card,
  .coach-card,
  .news-card {
	background: white;
	border-radius: 24px;
	padding: 28px 20px;
	text-align: center;
	transition: var(--transition);
	box-shadow: 0 10px 20px rgba(0,0,0,0.05);
  }
  
  .achievement-card:hover,
  .discipline-card:hover,
  .coach-card:hover,
  .news-card:hover {
	transform: translateY(-8px);
	box-shadow: var(--shadow);
  }
  
  .achievement-icon,
  .discipline-icon {
	font-size: 2.5rem;
	color: var(--gold);
	margin-bottom: 20px;
  }

  .discipline-card--image {
	background-position: center;
	background-size: cover;
	color: white;
	display: flex;
	flex-direction: column;
	justify-content: flex-end;
	min-height: 320px;
	padding-top: 120px !important;
	text-shadow: 0 2px 8px rgba(0,0,0,0.5);
  }

  .discipline-card--image .discipline-icon {
	color: white;
	text-shadow: 0 2px 8px rgba(0,0,0,0.6);
  }
  
  .gold-text {
	color: var(--gold);
	font-weight: 700;
  }

  .contact-title {
	color: #e8d89a;
  }
  
  .medal-badge {
	background: var(--gold);
	color: var(--black);
	display: inline-block;
	padding: 4px 12px;
	border-radius: 40px;
	font-size: 0.8rem;
	font-weight: bold;
	margin-top: 12px;
  }
  
  .coach-img {
	width: 120px;
	height: 120px;
	border-radius: 50%;
	object-fit: cover;
	margin-bottom: 16px;
	border: 3px solid var(--gold);
  }
  
  .coach-title {
	color: var(--red);
	font-weight: 600;
	margin: 8px 0;
  }
  
  /* События */
  .event-item {
	background: white;
	border-radius: 20px;
	padding: 20px;
	margin-bottom: 20px;
	display: flex;
	flex-wrap: wrap;
	justify-content: space-between;
	align-items: center;
	gap: 16px;
	box-shadow: 0 4px 10px rgba(0,0,0,0.05);
  }
  
  .event-date {
	background: var(--red);
	color: white;
	padding: 8px 18px;
	border-radius: 40px;
	font-weight: 700;
	min-width: 110px;
	text-align: center;
  }
  
  /* Контакты */
  .contacts-wrapper {
	display: grid;
	grid-template-columns: 1fr 1fr;
	gap: 48px;
	background: white;
	border-radius: 32px;
	padding: 48px;
	box-shadow: var(--shadow);
  }
  
  .contact-info i {
	color: var(--red);
	width: 32px;
	font-size: 1.4rem;
	margin-right: 16px;
  }
  
  .contact-item {
	display: flex;
	align-items: center;
	margin-bottom: 24px;
  }
  
  .social-links {
	display: flex;
	gap: 16px;
	margin-top: 24px;
	flex-wrap: wrap;
  }
  
  .social-links a {
	background: var(--black);
	color: white;
	width: 44px;
	height: 44px;
	border-radius: 50%;
	display: inline-flex;
	align-items: center;
	justify-content: center;
	font-size: 1.4rem;
	transition: var(--transition);
	text-decoration: none;
  }
  
  .social-links a:hover {
	background: var(--red);
	transform: translateY(-3px);
  }
  
  .contact-form input,
  .contact-form textarea {
	width: 100%;
	padding: 14px 18px;
	margin-bottom: 16px;
	border: 1px solid #ddd;
	border-radius: 30px;
	font-family: 'Open Sans', sans-serif;
	transition: 0.2s;
  }
  
  .contact-form input:focus,
  .contact-form textarea:focus {
	outline: none;
	border-color: var(--red);
	box-shadow: 0 0 0 2px rgba(178,34,34,0.2);
  }
  
  .contact-form button {
	background: var(--red);
	color: white;
	border: none;
	padding: 12px 28px;
	border-radius: 40px;
	font-weight: 600;
	cursor: pointer;
	transition: 0.2s;
  }
  
  .contact-form button:hover {
	background: var(--red-dark);
  }
  
  /* Футер (только если используется на главной, но стили для footer вынесены в headfoot.css) */
  
  /* Адаптивность */
  @media (max-width: 992px) {
	.contacts-wrapper {
	  grid-template-columns: 1fr;
	  padding: 32px;
	}
	.section-title {
	  font-size: 2rem;
	}
	.hero-content {
	  margin-left: 5%;
	  max-width: 80%;
	}
	.hero h1 {
	  font-size: 2.5rem;
	}
  }
  
  @media (max-width: 768px) {
	.hero-content {
	  margin-left: 20px;
	  max-width: 90%;
	}
	.stats-grid {
	  flex-direction: column;
	}
	.hero-buttons {
	  flex-direction: column;
	  align-items: flex-start;
	}
	.event-item {
	  flex-direction: column;
	  align-items: flex-start;
	}
  }